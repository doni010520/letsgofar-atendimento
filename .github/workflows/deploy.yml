name: Build & Push Docker image

on:
  push:
    branches: [master]
  workflow_dispatch:

env:
  IMAGE: ghcr.io/${{ github.repository }}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      # Lê APP_VERSION (ex.: v2.19.0) pra taguear a imagem com nome legível.
      - name: Extract app version
        id: ver
        run: echo "v=$(grep -oE 'v[0-9]+\.[0-9]+\.[0-9]+' src/lib/version.ts | head -1)" >> "$GITHUB_OUTPUT"

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE }}:latest
            ${{ env.IMAGE }}:${{ github.sha }}
            ${{ env.IMAGE }}:${{ steps.ver.outputs.v }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            GIT_SHA=${{ github.sha }}
            NEXT_PUBLIC_SUPABASE_URL=https://xzhzbefkxfgvwfqztqan.supabase.co
            NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inh6aHpiZWZreGZndndmcXp0cWFuIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODA3NjUwNjUsImV4cCI6MjA5NjM0MTA2NX0.wyqbuikHIHQaU9Vdu1dGxw6pw4wlDD1K_O1tx84TFEU
            NEXT_PUBLIC_META_GRAPH_VERSION=v23.0
            NEXT_PUBLIC_META_APP_ID=1501308275071442
